/**
 * 计算两个坐标的中点坐标
 * @param start 起始点{x:number, y:number}
 * @param end 结束点{x:number, y:number}
 * @returns 中点坐标{x:number, y:number}
 */
declare function getMiddlePoint(start: any, end: any): {
    x: any;
    y: any;
};
export { getMiddlePoint };
